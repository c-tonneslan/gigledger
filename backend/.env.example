DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gigledger
# Falls back to SQLite if DATABASE_URL is unset, so you can run without Postgres locally.
ANTHROPIC_API_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
USER_STATE=PA
USER_FILING_STATUS=single
